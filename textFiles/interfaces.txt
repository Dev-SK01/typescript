--------------------------- INTERFACES -------------

1. interfaces are predefined ts type structure,

2. used to reduce the time of the reused types in ts , if we define the type we can use it any where

3. we can also use optional types.

4. interface extends  , implements are used to create more interfaces

5. if we create a interface with same name it merge the both interfaces